<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <ChangelogFile>$(MSBuildThisFileDirectory)CHANGELOG.md</ChangelogFile>
    <TargetFramework>netstandard2.0</TargetFramework>
    <Description>Companion library to format using fantomas tool.</Description>
    <IsPackable>true</IsPackable>
    <ProduceReferenceAssembly>true</ProduceReferenceAssembly>
  </PropertyGroup>
  <ItemGroup>
    <!-- The daemon cache in LSPFantomasService is internal, and its tests live next door. -->
    <InternalsVisibleTo Include="Fantomas.Client.Tests" />
  </ItemGroup>
  <ItemGroup>
    <None Include="CHANGELOG.md" />
    <Compile Include="Contracts.fsi" />
    <Compile Include="Contracts.fs" />
    <Compile Include="LSPFantomasServiceTypes.fsi" />
    <Compile Include="LSPFantomasServiceTypes.fs" />
    <Compile Include="FantomasToolLocator.fsi" />
    <Compile Include="FantomasToolLocator.fs" />
    <Compile Include="LSPFantomasService.fsi" />
    <Compile Include="LSPFantomasService.fs" />
  </ItemGroup>
  <ItemGroup>
    <!--
    A published library, so its FSharp.Core is the lowest version consumers must have rather than
    the newest one available. `VersionOverride` holds it below what the rest of the solution uses.
    -->
    <PackageReference Include="FSharp.Core" VersionOverride="10.0.100" />
    <PackageReference Include="StreamJsonRpc" />
    <PackageReference Include="SemanticVersioning" />
  </ItemGroup>
</Project>